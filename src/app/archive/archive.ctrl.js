archive.controller('archiveCtrl', [
    // '$rootScope',
    '$scope',
    function($scope) {
        // $scope.archive = $rootScope.archive;
        // console.log('test');
        $scope.testStr = 'Works!';
    }
]);